"""Contains logging implementation"""

# TODO:
#   - User log handler - log in file f"{hash(user_id + str(user.chat_id))}.log
#   - All system logs in a separate file
#   - Check files size - too big => start a new one (big is 300Mb)
